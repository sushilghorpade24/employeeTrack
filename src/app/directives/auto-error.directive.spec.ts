import { AutoErrorDirective } from './auto-error.directive';

describe('AutoErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoErrorDirective();
    expect(directive).toBeTruthy();
  });
});
